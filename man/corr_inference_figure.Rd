% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan-figures-generate_inference_figure.R
\name{corr_inference_figure}
\alias{corr_inference_figure}
\title{Generates an interval plot for correlation/covariance matrix elements}
\usage{
corr_inference_figure(inference.df, conf = 0.95, type = "marginal.hpd",
  corr = TRUE, title = NULL, axis.ticks.x = TRUE,
  axis.text.size.x = NULL, ylimits = NULL, segment_size = 2,
  verbose = FALSE, num_level = 0, text.size = 20)
}
\arguments{
\item{inference.df}{The data frame to use in plotting. Must have the
following columns:
\itemize{
  \item \code{median}: The median posterior value for each element
  \item \code{true}: The true value for each element
  \item \code{lower},\code{upper}: The upper and lower credible interval
    limits for each element
  \item \code{x}: The x-axis label for each element
  \item \code{xmin}, \code{xmax}: The minimum and maximum x-axis values
    for the bounding box of each element
  \item \code{xmin.t},\code{xmax.t}: The minimum and maximum x-axis values
    for the true value and estimate tick marks
}
Can optionally have a column called \code{comp}, which is the
compositional correlation for each element. If \code{corr} is \code{TRUE},
must also have the columns:
\itemize{
  \item \code{var.type}: "variance" or "covariance" depending on whether
    the element is on the diagonal or not
}}

\item{conf}{
\%\%     ~~Describe \code{conf} here~~
Scalar, the confidence level desired.
}

\item{type}{One of \code{"marginal.centered"} or \code{"marginal.hpd"},
indicating whether centeral or highest posterior density intervals are
desired}

\item{corr}{Boolean: Are these correlation matrices?}

\item{title}{A title for the plot}

\item{axis.ticks.x}{A boolean indicating whether to put the coordinate
values on the x-axis.}

\item{axis.text.size.x}{If specified when \code{x.axis.ticks} is \code{TRUE},
then gives the text size of the x-axis tick labels.}

\item{ylimits}{A vector of length two giving the lower and upper limits of
the y-axis}

\item{segment_size}{The size of the segments drawn in the plot}

\item{verbose}{Boolean: print informative statements as
function runs?}

\item{num_level}{The number of the level (will determine the number of spaces to add)}

\item{text.size}{The size of the plot text to use}
}
\value{
Returns a \code{ggplot} plot.
}
\description{
Generates an interval plot for correlation/covariance matrix elements
}

